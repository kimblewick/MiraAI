# /data – Data Requirements & Schema

This directory contains data-related planning documents:

- DynamoDB table schema (UserProfiles, Conversations)
- Sample payloads for external Astrology API
- Example profile creation requests
- Example chat messages and AI responses
- Any transformations or normalization rules

This folder does not contain runtime data — only design-time artifacts.